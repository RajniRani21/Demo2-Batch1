name: learn-github-actions

on: push
env:
      ## Environment variable
  name1: "rajni"
  name2: "rani"
jobs:
 job1:
  runs-on: self-hosted
  steps:
    - name: envirment variable
      run: echo"The name1 is:$name1 $name2"
